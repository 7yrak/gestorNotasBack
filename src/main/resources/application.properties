spring.application.name=gestor-notas
server.port=8082

# Configuración de base de datos
spring.datasource.url=jdbc:postgresql://10.51.9.50:5432/gestor_notas?stringtype=unspecified
spring.datasource.username=sergiofica
spring.datasource.password=local1234$
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optimización para Java 21 (Habilitar hilos virtuales para Tomcat)
spring.threads.virtual.enabled=true
